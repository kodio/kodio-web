---
import Image from 'components/Image.astro';
import SanityLink from 'components/sanity/SanityLink.astro';
import SectionLayout from 'layouts/SectionLayout.astro';
import type { Header } from 'types/sanity';

export interface Props {
  header: Header;
}

const { header } = Astro.props;

const link = header.links?.[0];
---

<SectionLayout color={header.color}>
  <div class="header-container">
    <div class="heading-wrapper">
      <h1>{header.heading}</h1>
      {link && <SanityLink link={link} />}
    </div>
    {header.image && <Image image={header.image} />}
  </div>
</SectionLayout>

<style lang="scss">
  .header-container {
    padding-top: 5rem;
    display: grid;
    gap: 2rem;

    @media (min-width: $width-l) {
      grid-template-columns: 4fr 3fr;
    }

    .heading-wrapper {
      display: grid;
      gap: 2rem;

      h1 {
        font-size: clamp(200%, calc(100vw / 20), 400%);
      }

      :global(a) {
        @include button(var(--pale-yellow));
        text-decoration: none;
        justify-self: start;
        padding: 0.5rem 2rem;
        font-size: 150%;
        font-weight: bolder;
      }
    }

    .header-image {
      margin: auto 0;
    }
  }
</style>
