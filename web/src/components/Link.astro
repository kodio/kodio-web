---
import type { HTMLAttributes } from 'astro/types';

export type Props = Omit<HTMLAttributes<'a'>, 'href'> & { href: string };

const { href, ...rest } = Astro.props;
---

<a href={href} rel="prefetch-intent" {...rest}><slot /></a>
