<ul role="list" class="unordered-list">
  <slot />
</ul>

<style lang="scss">
  .unordered-list {
    margin-bottom: 1rem;
    padding-left: 24px;
    list-style: none;

    :global(li) {
      position: relative;

      &:not(:last-child) {
        margin-bottom: 0.5rem;
      }

      &::before {
        content: '';
        height: 6px;
        width: 6px;
        position: absolute;
        left: -18px;
        top: 11px;
        border-radius: 50%;
        background-color: var(--kodio-dark);
      }
    }
  }
</style>
