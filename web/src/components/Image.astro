---
import { urlFor } from 'lib/sanity';
import type { ImageObj } from 'types/sanity';

export interface Props {
  image: ImageObj;
  class?: string;
}

const { image } = Astro.props;
---

<img class={Astro.props.class} src={urlFor(image).url()} alt={image.alt ?? ''} loading="lazy" />
