<ol role="list" class="ordered-list">
  <slot />
</ol>

<style lang="scss">
  .ordered-list {
    counter-reset: list-number;
    padding-left: 1.8em;
    list-style: none;

    :global(li) {
      position: relative;

      &:not(:last-child) {
        margin-bottom: 0.5rem;
      }

      &::before {
        counter-increment: list-number;
        content: counter(list-number) '.';
        font-family: var(--ff-lora);
        font-size: 125%;
        line-height: 1.3;
        height: 1em;
        position: absolute;
        left: -1.3em;
        top: 0;
        color: var(--orange-500);
      }
    }
  }
</style>
