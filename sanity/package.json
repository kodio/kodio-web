{
  "name": "kodio-sanity-tmp",
  "version": "1.0.0",
  "private": true,
  "license": "ISC",
  "description": "Sanity for website for Kodio",
  "scripts": {
    "start": "sanity start",
    "build": "sanity build",
    "deploy": "sanity deploy",
    "deploy-graphql": "sanity graphql deploy",
    "lint": "yarn prettier:check && eslint . --max-warnings 0",
    "prettier:check": "prettier -l ."
  },
  "dependencies": {
    "react": "^17.0.0",
    "react-dom": "^17.0.0",
    "react-is": "^17.0.0",
    "sanity": "dev-preview",
    "styled-components": "^5.2.0"
  },
  "devDependencies": {
    "@sanity/eslint-config-studio": "^2.0.0",
    "eslint": "^8.6.0",
    "prettier": "^2.7.1",
    "typescript": "^4.0.0"
  }
}
