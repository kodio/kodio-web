{
  "name": "kodio-web",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for kodio web",
  "author": "Kodio",
  "scripts": {
    "dev": "yarn --cwd web dev",
    "lint": "yarn --cwd web lint && yarn --cwd sanity lint",
    "sanity": "yarn --cwd sanity start"
  },
  "devDependencies": {
    "prettier": "^2.7.1"
  }
}
