{
  "name": "kodio-web",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for kodio web",
  "author": "Kodio",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "sanity": "npm run start --prefix sanity",
    "verify": "npm run verify --prefix web && npm run verify --prefix sanity",
    "postinstall": "npm ci --prefix web && npm ci --prefix sanity"
  },
  "devDependencies": {
    "prettier": "^2.7.1"
  }
}
