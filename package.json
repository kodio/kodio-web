{
  "name": "kodio-web",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for kodio web",
  "author": "Kodio",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "lint": "npm run lint --prefix web && npm run lint --prefix sanity",
    "sanity": "npm run start --prefix sanity",
    "postinstall": "npm install --prefix web && npm install --prefix sanity"
  },
  "devDependencies": {
    "prettier": "^2.7.1"
  }
}
