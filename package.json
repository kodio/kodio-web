{
  "name": "kodio-web-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for kodio web",
  "author": "Kodio",
  "homepage": "https://kodio.no",
  "repository": {
    "type": "git",
    "url": "https://github.com/Glitner/kodio-web"
  },
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web",
    "cms": "npm run start --prefix cms",
    "cms:deploy": "npm run deploy --prefix cms",
    "verify": "npm run verify --prefix web && npm run verify --prefix cms",
    "postinstall": "npm ci --prefix web && npm ci --prefix cms"
  },
  "devDependencies": {
    "prettier": "^2.7.1"
  }
}
